import { DigitalFiles } from "@/components/DigitalFiles";

export const metadata = {
  title: "Digital Files — E-Portfolio",
  description: "Upload and view presentation files.",
};

export default function DigitalFilesPage() {
  return <DigitalFiles />;
}
