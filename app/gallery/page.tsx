import { GalleryGrid } from "@/components/GalleryGrid";

export const metadata = {
  title: "Portfolio — E-Portfolio",
  description: "Browse all portfolio documents.",
};

export default function GalleryPage() {
  return <GalleryGrid />;
}
